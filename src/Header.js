import React from 'react'
import './Header.css'

function Header() {
    return(
        <header>
            <h2>CORONAVÍRUS BRASIL</h2>
            <select>
                <option value="br">Mundo</option>
            </select>
        </header>
    );
}

export default Header;